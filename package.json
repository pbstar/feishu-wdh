{
  "name": "feishu-wdh",
  "version": "0.3.1",
  "description": "一键将飞书文档经 AI 优化后导出为 Markdown，图片本地化并打包 ZIP，可选前端研发任务清单",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc && vite build && node scripts/pack.mjs"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.2.2",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
