# 更新日志

本项目所有值得记录的变更都会写入此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.1] - 2026-08-06

- 优化：清理「AI 文档优化为必开功能」的表述——开关早已移除，AI 优化就是导出流程本身，无需再强调「必开」；README、设置页文案、代码注释一并统一
- 优化：设置页提示文案改为聚焦配置本身（「请先点击 ⚙ 完成 AI API 配置」/「✓ AI 配置已完成」），不再重复「已开启」

## [0.3.0] - 2026-08-05

- 优化：AI 文档优化与前端研发任务清单改为**并行**执行，总耗时从二者之和降为二者最大值，大幅缩短已开启任务总结时的导出等待
- 优化：并发 AI 请求共享一条进度心跳上报，避免多个请求各自上报相同文案导致界面闪烁
- 重构：支线任务抽象为注册表（`src/ai/extras.ts`），`AiConfig` 改为结构化 `extras` 开关对象；后续新增支线任务只需追加注册表条目与提示词，导出流程、存储与设置页 UI 均无需改动
- 重构：设置页支线开关改为注册表数据驱动渲染；`sanitizeFilename` 抽至 `shared/` 消除打包冗余
- 变更：`AiConfig` 配置结构升级为 `extras` 对象，不再兼容旧版平级开关字段；旧存储下的任务总结开关需在设置页重新开启

## [0.2.2] - 2026-08-05

- 修复：文档无图片（或图片全部抓取失败）时，ZIP 不再生成空的 `images/` 文件夹
- 优化：强化 AI 文档优化提示词，明确「逐段精简、去冗余」为主、排版优化为辅，避免优化结果与原文近乎相同
- 优化：AI 请求补充温和温度参数（temperature=0.5），让优化结果更稳定

## [0.2.1] - 2026-08-05

- 新增：导出进行中关闭弹窗后重新打开，自动恢复显示当前导出进度（阶段、消息、进度条），按钮保持禁用，避免误以为导出已中止
- 新增：导出结束后重新打开弹窗，自动恢复显示上次导出结果（完成/失败），结果持久化于本地存储，即使后台被回收也能恢复
- 优化：后台实时跟踪 content 滚动/抽取与 AI 心跳的最新进度文案，弹窗重开恢复时显示即时进度

## [0.2.0] - 2026-08-04

- 变更：AI 文档优化成为导出主输出，ZIP 不再包含原文 Markdown，仅导出 AI 优化版 `文档标题.md`（文件名不再带 `-AI优化` 后缀）
- 变更：AI 优化为必开功能，未配置 API 时导出直接报错并引导前往设置页
- 变更：AI 优化失败不再降级导出原文，改为整个导出失败并提示错误原因
- 保留：AI 前端研发任务总结为可选附加项（默认关闭），其生成失败仅跳过该文件，不影响主导出
- 移除：AI 文档优化开关及 `AiConfig.enabled` 配置，相关代码与 UI 一并清理

## [0.1.0] - 2026-08-03

- 重构：版本升级至 0.1.0，导出默认仅包含原文 Markdown
- 新增：AI 文档优化开关——开启后额外导出一份逐段精简的 AI 优化版 Markdown（`文档标题-AI优化.md`）
- 新增：AI 前端研发任务总结开关——开启后基于需求文档额外生成一份前端研发任务清单（`文档标题-任务清单.md`）
- 移除：总结粒度（精简摘要 / 仅去冗余）选项，统一采用逐段精简；相关配置与代码一并清理

## [0.0.4] - 2026-08-03

- 优化：AI 请求超时放宽至 5 分钟，适配长文档处理耗时
- 优化：AI 长请求期间周期性上报已等待时长，消息同时重置 service worker 生命周期，避免长导出被静默回收，界面也不再长时间无反馈
- 优化：合并向 popup 发送进度/结果的消息封装

## [0.0.3] - 2026-08-03

- 修复：AI 请求移入 offscreen document 执行，规避 MV3 service worker 生命周期回收导致的导出永久卡在「正在调用 AI 处理内容」；请求超时后仍会明确报错并降级导出原文

## [0.0.2] - 2026-07-31

- 修复：AI 总结超时未覆盖响应体读取，界面可能长时间卡在「正在调用 AI 处理内容」；超时放宽至 120 秒，超时自动降级导出原文
- 优化：精简 manifest 权限，移除未使用的 `activeTab`、`scripting`
- 优化：后台导出增加互斥保护，避免重复触发并发导出
- 优化：抽取重复的进度上报逻辑至 shared；构建与打包脚本精简

## [0.0.1] - 2026-07-30

- 初始化版本
